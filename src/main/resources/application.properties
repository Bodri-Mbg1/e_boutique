# ? Configuration du port
server.port=8080

# ?? Configuration de la base PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/e-boutique
spring.datasource.username=postgres
spring.datasource.password=123456789
spring.datasource.driver-class-name=org.postgresql.Driver


# ? Hibernate (mise ï¿½ jour automatique des tables)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ? Encodage
spring.datasource.hikari.connection-init-sql=SET NAMES 'UTF8'
spring.http.encoding.charset=UTF-8
spring.http.encoding.enabled=true
spring.http.encoding.force=true

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mbgbodri@gmail.com
spring.mail.password=qtyb zvjv igyq mgex
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true


# ? Taille minimale et maximale du pool
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.maximum-pool-size=20

# ? Temps max d?attente pour obtenir une connexion (en ms)
spring.datasource.hikari.connection-timeout=30000

# ? Durée max de vie d?une connexion (en ms)
spring.datasource.hikari.max-lifetime=1800000

# ? Fréquence à laquelle Hikari vérifie l?état du pool (ms)
spring.datasource.hikari.idle-timeout=600000

# ? Activation du test de validité des connexions (optionnel)
spring.datasource.hikari.validation-timeout=5000
spring.datasource.hikari.connection-test-query=SELECT 1



