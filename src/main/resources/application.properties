# Configuration du port
server.port=9091

# Configuration de la base PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/eboutique
spring.datasource.username=eboutiqueuser
spring.datasource.password=123457
spring.datasource.driver-class-name=org.postgresql.Driver


# Hibernate (mise jour automatique des tables)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Encodage
spring.datasource.hikari.connection-init-sql=SET NAMES 'UTF8'
spring.http.encoding.charset=UTF-8
spring.http.encoding.enabled=true
spring.http.encoding.force=true

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mbgbodri@gmail.com
spring.mail.password=qtyb zvjv igyq mgex
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true


# Taille minimale et maximale du pool
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.maximum-pool-size=20

# Temps max d attente pour obtenir une connexion (en ms)
spring.datasource.hikari.connection-timeout=30000

# Duree max de vie d une connexion (en ms)
spring.datasource.hikari.max-lifetime=1800000

# Frequence a laquelle Hikari vérifie letat du pool (ms)
spring.datasource.hikari.idle-timeout=600000

# Activation du test de validite des connexions (optionnel)
spring.datasource.hikari.validation-timeout=5000
spring.datasource.hikari.connection-test-query=SELECT 1



